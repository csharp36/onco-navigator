---
phase: 03-working-application
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/patients/types.ts
  - frontend/src/features/alerts/types.ts
  - frontend/src/features/dashboard/types.ts
  - frontend/src/features/patients/api.ts
  - frontend/src/features/alerts/api.ts
  - frontend/src/features/dashboard/api.ts
  - frontend/src/routes/patients/index.tsx
  - frontend/src/routes/patients/new.tsx
  - frontend/src/routes/patients/$patientId.tsx
  - frontend/src/routes/alerts/index.tsx
autonomous: true
requirements: [ALRT-05, ALRT-06]

must_haves:
  truths:
    - "TypeScript types for all API request/response shapes exist and match backend DTOs"
    - "TanStack Query hooks exist for all patient, alert, and dashboard API calls"
    - "Alert count hook uses 30-second polling via refetchInterval"
    - "Route files exist for /patients, /patients/new, /patients/$patientId, /alerts"
    - "Additional shadcn components (select, textarea, label, form, table) are installed"
  artifacts:
    - path: "frontend/src/features/patients/types.ts"
      provides: "TypeScript interfaces matching backend DTOs"
      contains: "PatientResponse"
    - path: "frontend/src/features/alerts/api.ts"
      provides: "TanStack Query hooks for alerts with polling"
      contains: "refetchInterval"
    - path: "frontend/src/routes/patients/$patientId.tsx"
      provides: "Dynamic route for patient detail"
      contains: "createFileRoute"
  key_links:
    - from: "features/patients/api.ts"
      to: "lib/api-client.ts"
      via: "apiClient import"
      pattern: "apiClient"
    - from: "features/alerts/api.ts"
      to: "lib/api-client.ts"
      via: "apiClient import"
      pattern: "refetchInterval.*30_000"
---

<objective>
Create the frontend foundation: install additional shadcn components, define TypeScript types matching all backend DTOs, create TanStack Query hooks for every API endpoint, and scaffold all route files.

Purpose: Establish the frontend contracts and data-fetching layer so that page component plans (Plans 04 and 05) can focus purely on UI composition without needing to define types or hooks.

Output: 3 type files, 3 api hook files, 4 route scaffolds, additional shadcn components installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-working-application/03-RESEARCH.md
@.planning/phases/03-working-application/03-PATTERNS.md
@.planning/phases/03-working-application/03-UI-SPEC.md

<interfaces>
<!-- Existing frontend interfaces the executor needs -->

From frontend/src/lib/api-client.ts:
```typescript
export class ApiError extends Error { status: number; }
export const apiClient = {
  get: <T>(path: string) => request<T>(path),
  post: <T>(path: string, body: unknown) => request<T>(path, { method: 'POST', body: JSON.stringify(body) }),
  patch: <T>(path: string, body: unknown) => request<T>(path, { method: 'PATCH', body: JSON.stringify(body) }),
  delete: <T>(path: string) => request<T>(path, { method: 'DELETE' }),
};
```

From frontend/src/lib/auth.ts:
```typescript
export function getAccessToken(): string | null;
export function hasRole(role: string): boolean;
export function getUserName(): string;
export function isAuthenticated(): boolean;
```

From frontend/src/routes/index.tsx (existing dashboard placeholder):
```typescript
import { createFileRoute } from '@tanstack/react-router';
export const Route = createFileRoute('/')({ component: DashboardPage });
```

From frontend/src/components/ui/ (existing components):
alert.tsx, badge.tsx, button.tsx, card.tsx, dialog.tsx, input.tsx, separator.tsx, sheet.tsx, sidebar.tsx, skeleton.tsx, tooltip.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and create TypeScript types for all API contracts</name>
  <files>
    frontend/src/features/patients/types.ts,
    frontend/src/features/alerts/types.ts,
    frontend/src/features/dashboard/types.ts
  </files>
  <read_first>
    frontend/package.json,
    frontend/components.json,
    frontend/src/lib/api-client.ts
  </read_first>
  <action>
**Install shadcn components** (run from `frontend/` directory):
```bash
cd frontend && npx shadcn@latest add select textarea label form table tabs popover command -y
```
This adds 8 components to `frontend/src/components/ui/`. The `-y` flag auto-accepts prompts.

**Create directory structure:**
```
frontend/src/features/patients/
frontend/src/features/alerts/
frontend/src/features/dashboard/
```

**frontend/src/features/patients/types.ts:**
```typescript
export interface PatientResponse {
  id: string;
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  mrn: string;
  cancerType: 'BREAST' | 'LUNG' | 'COLORECTAL';
  cancerStage: string;
  diagnosisDate: string;
  assignedNavigatorId: string | null;
  treatingPhysician: string | null;
  status: 'ACTIVE' | 'INACTIVE';
  summaryStatus: 'On Track' | 'Alert Active' | 'Inactive';
  createdAt: string;
}

export interface CreatePatientRequest {
  firstName: string;
  lastName: string;
  dateOfBirth: string;
  mrn: string;
  cancerType: 'BREAST' | 'LUNG' | 'COLORECTAL';
  cancerStage: string;
  diagnosisDate: string;
  assignedNavigatorId?: string;
  treatingPhysician?: string;
}

export interface CareEventResponse {
  id: string;
  patientId: string;
  eventType: string;
  eventDate: string;
  status: 'SCHEDULED' | 'COMPLETED' | 'CANCELLED' | 'PENDING';
  notes: string | null;
  pathwayStepId: string | null;
  createdAt: string;
}

export interface CreateCareEventRequest {
  eventType: string;
  eventDate: string;
  status: 'SCHEDULED' | 'COMPLETED' | 'CANCELLED' | 'PENDING';
  notes?: string;
}

export interface UpdateCareEventStatusRequest {
  status: 'SCHEDULED' | 'COMPLETED' | 'CANCELLED' | 'PENDING';
}

export interface DeactivatePatientRequest {
  reason: string;
}

export interface PathwayStatusResponse {
  patientId: string;
  steps: PathwayStepStatus[];
}

export interface PathwayStepStatus {
  stepId: string;
  stepNumber: number;
  stepName: string;
  status: 'COMPLETED' | 'OVERDUE' | 'MISSING' | 'UPCOMING';
  completionDate: string | null;
  timingInfo: string;
  hasActiveAlert: boolean;
}
```

**frontend/src/features/alerts/types.ts:**
```typescript
export interface AlertResponse {
  id: string;
  patientId: string;
  patientName: string;
  patientMrn: string;
  alertType: 'DELAYED_EVENT' | 'MISSING_EVENT' | 'OUT_OF_ORDER';
  severityLabel: 'OVERDUE' | 'MISSING' | 'OUT OF ORDER';
  status: 'OPEN' | 'ACKNOWLEDGED' | 'RESOLVED';
  pathwayStepName: string;
  deviationDescription: string;
  suggestedAction: string;
  createdAt: string;
  timeElapsed: string;
}

export interface ResolveAlertRequest {
  notes: string;
}

export interface AlertCountResponse {
  count: number;
}
```

**frontend/src/features/dashboard/types.ts:**
```typescript
import type { AlertResponse } from '../alerts/types';

export interface DashboardStatsResponse {
  openAlertCount: number;
  activePatients: number;
  onTrackPatients: number;
  topUrgentAlerts: AlertResponse[];
}
```
  </action>
  <verify>
    <automated>cd /Users/csharpl/Desktop/Source_Code/Java/Onco-Navigator/frontend && ls src/components/ui/select.tsx src/components/ui/textarea.tsx src/components/ui/table.tsx src/features/patients/types.ts src/features/alerts/types.ts src/features/dashboard/types.ts 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/select.tsx exists (shadcn component installed)
    - frontend/src/components/ui/textarea.tsx exists (shadcn component installed)
    - frontend/src/components/ui/table.tsx exists (shadcn component installed)
    - frontend/src/features/patients/types.ts contains `interface PatientResponse` with `summaryStatus` field
    - frontend/src/features/patients/types.ts contains `interface CreatePatientRequest` with all D-01 wizard fields
    - frontend/src/features/patients/types.ts contains `interface PathwayStepStatus` with `hasActiveAlert` field
    - frontend/src/features/alerts/types.ts contains `interface AlertResponse` with `severityLabel` and `alertType`
    - frontend/src/features/alerts/types.ts contains `interface AlertCountResponse` with `count` field
    - frontend/src/features/dashboard/types.ts contains `interface DashboardStatsResponse` with `topUrgentAlerts`
  </acceptance_criteria>
  <done>All shadcn components installed. TypeScript types defined for every API request/response shape matching backend DTOs exactly. Types include severity labels, summary status, pathway step status, and alert count.</done>
</task>

<task type="auto">
  <name>Task 2: TanStack Query hooks for all API endpoints and route file scaffolds</name>
  <files>
    frontend/src/features/patients/api.ts,
    frontend/src/features/alerts/api.ts,
    frontend/src/features/dashboard/api.ts,
    frontend/src/routes/patients/index.tsx,
    frontend/src/routes/patients/new.tsx,
    frontend/src/routes/patients/$patientId.tsx,
    frontend/src/routes/alerts/index.tsx
  </files>
  <read_first>
    frontend/src/lib/api-client.ts,
    frontend/src/routes/index.tsx,
    frontend/src/routes/login.tsx,
    frontend/src/features/patients/types.ts,
    frontend/src/features/alerts/types.ts,
    frontend/src/features/dashboard/types.ts
  </read_first>
  <action>
**frontend/src/features/patients/api.ts** — TanStack Query hooks following RESEARCH.md Pattern 5 key hierarchy:
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { apiClient } from '@/lib/api-client';
import type {
  PatientResponse, CreatePatientRequest, CareEventResponse,
  CreateCareEventRequest, UpdateCareEventStatusRequest,
  DeactivatePatientRequest, PathwayStatusResponse,
} from './types';

export function usePatients(mrn?: string) {
  return useQuery({
    queryKey: mrn ? ['patients', { mrn }] : ['patients'],
    queryFn: () => apiClient.get<PatientResponse[]>(
      mrn ? `/patients?mrn=${encodeURIComponent(mrn)}` : '/patients'
    ),
  });
}

export function usePatient(patientId: string) {
  return useQuery({
    queryKey: ['patients', patientId],
    queryFn: () => apiClient.get<PatientResponse>(`/patients/${patientId}`),
  });
}

export function useCreatePatient() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreatePatientRequest) =>
      apiClient.post<PatientResponse>('/patients', data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['patients'] });
      queryClient.invalidateQueries({ queryKey: ['dashboard', 'stats'] });
    },
  });
}

export function useDeactivatePatient() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ patientId, data }: { patientId: string; data: DeactivatePatientRequest }) =>
      apiClient.patch<void>(`/patients/${patientId}/deactivate`, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['patients'] });
      queryClient.invalidateQueries({ queryKey: ['dashboard', 'stats'] });
      queryClient.invalidateQueries({ queryKey: ['alerts'] });
      queryClient.invalidateQueries({ queryKey: ['alerts', 'count'] });
    },
  });
}

export function useCareEvents(patientId: string) {
  return useQuery({
    queryKey: ['patients', patientId, 'care-events'],
    queryFn: () => apiClient.get<CareEventResponse[]>(`/patients/${patientId}/care-events`),
  });
}

export function useCreateCareEvent(patientId: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (data: CreateCareEventRequest) =>
      apiClient.post<CareEventResponse>(`/patients/${patientId}/care-events`, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['patients', patientId, 'care-events'] });
      queryClient.invalidateQueries({ queryKey: ['patients', patientId, 'pathway-status'] });
      queryClient.invalidateQueries({ queryKey: ['patients'] });
      queryClient.invalidateQueries({ queryKey: ['alerts'] });
      queryClient.invalidateQueries({ queryKey: ['alerts', 'count'] });
      queryClient.invalidateQueries({ queryKey: ['dashboard', 'stats'] });
    },
  });
}

export function useUpdateCareEventStatus(patientId: string) {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ careEventId, data }: { careEventId: string; data: UpdateCareEventStatusRequest }) =>
      apiClient.patch<CareEventResponse>(`/patients/${patientId}/care-events/${careEventId}`, data),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['patients', patientId, 'care-events'] });
      queryClient.invalidateQueries({ queryKey: ['patients', patientId, 'pathway-status'] });
      queryClient.invalidateQueries({ queryKey: ['alerts'] });
      queryClient.invalidateQueries({ queryKey: ['alerts', 'count'] });
    },
  });
}

export function usePathwayStatus(patientId: string) {
  return useQuery({
    queryKey: ['patients', patientId, 'pathway-status'],
    queryFn: () => apiClient.get<PathwayStatusResponse>(`/patients/${patientId}/pathway-status`),
  });
}
```

**frontend/src/features/alerts/api.ts** — polling hooks per D-12:
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { apiClient } from '@/lib/api-client';
import type { AlertResponse, ResolveAlertRequest, AlertCountResponse } from './types';

export function useAlerts() {
  return useQuery({
    queryKey: ['alerts'],
    queryFn: () => apiClient.get<AlertResponse[]>('/alerts'),
    refetchInterval: 30_000,
  });
}

export function useAlertCount() {
  return useQuery({
    queryKey: ['alerts', 'count'],
    queryFn: () => apiClient.get<AlertCountResponse>('/alerts/count'),
    refetchInterval: 30_000,
    staleTime: 0,
  });
}

export function useResolveAlert() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ alertId, notes }: { alertId: string; notes: string }) =>
      apiClient.post<void>(`/alerts/${alertId}/resolve`, { notes } as ResolveAlertRequest),
    onSuccess: async () => {
      await Promise.all([
        queryClient.invalidateQueries({ queryKey: ['alerts', 'count'] }),
        queryClient.invalidateQueries({ queryKey: ['alerts'] }),
        queryClient.invalidateQueries({ queryKey: ['dashboard', 'stats'] }),
      ]);
    },
  });
}
```

**frontend/src/features/dashboard/api.ts:**
```typescript
import { useQuery } from '@tanstack/react-query';
import { apiClient } from '@/lib/api-client';
import type { DashboardStatsResponse } from './types';

export function useDashboardStats() {
  return useQuery({
    queryKey: ['dashboard', 'stats'],
    queryFn: () => apiClient.get<DashboardStatsResponse>('/dashboard/stats'),
    refetchInterval: 30_000,
  });
}
```

**Route file scaffolds** — create minimal route files so TanStack Router generates routeTree.gen.ts. Each route renders a placeholder that will be replaced by actual page components in Plans 04/05.

**frontend/src/routes/patients/index.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/patients/')({
  component: PatientListPage,
});

function PatientListPage() {
  return <div className="p-6"><h1 className="text-3xl font-semibold tracking-tight">Patients</h1><p className="text-muted-foreground">Loading patient list...</p></div>;
}
```

**frontend/src/routes/patients/new.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/patients/new')({
  component: NewPatientPage,
});

function NewPatientPage() {
  return <div className="p-6"><h1 className="text-3xl font-semibold tracking-tight">Add Patient</h1><p className="text-muted-foreground">Loading wizard...</p></div>;
}
```

**frontend/src/routes/patients/$patientId.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/patients/$patientId')({
  component: PatientDetailPage,
});

function PatientDetailPage() {
  const { patientId } = Route.useParams();
  return <div className="p-6"><h1 className="text-3xl font-semibold tracking-tight">Patient Detail</h1><p className="text-muted-foreground">Loading patient {patientId}...</p></div>;
}
```

**frontend/src/routes/alerts/index.tsx:**
```typescript
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/alerts/')({
  component: AlertQueuePage,
});

function AlertQueuePage() {
  return <div className="p-6"><h1 className="text-3xl font-semibold tracking-tight">Alerts</h1><p className="text-muted-foreground">Loading alerts...</p></div>;
}
```

After creating all route files, run `npm run dev` briefly (or `npx vite build`) from the `frontend/` directory to regenerate `routeTree.gen.ts`, then verify `tsc -b` succeeds.
  </action>
  <verify>
    <automated>cd /Users/csharpl/Desktop/Source_Code/Java/Onco-Navigator/frontend && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/patients/api.ts contains `usePatients`, `usePatient`, `useCreatePatient`, `useDeactivatePatient`, `useCareEvents`, `useCreateCareEvent`, `useUpdateCareEventStatus`, `usePathwayStatus`
    - frontend/src/features/alerts/api.ts contains `useAlerts` with `refetchInterval: 30_000`
    - frontend/src/features/alerts/api.ts contains `useAlertCount` with `refetchInterval: 30_000` and `staleTime: 0`
    - frontend/src/features/alerts/api.ts contains `useResolveAlert` with `invalidateQueries` for `['alerts']`, `['alerts', 'count']`, and `['dashboard', 'stats']`
    - frontend/src/features/dashboard/api.ts contains `useDashboardStats` with `refetchInterval: 30_000`
    - frontend/src/routes/patients/index.tsx contains `createFileRoute('/patients/')`
    - frontend/src/routes/patients/new.tsx contains `createFileRoute('/patients/new')`
    - frontend/src/routes/patients/$patientId.tsx contains `createFileRoute('/patients/$patientId')` and `Route.useParams()`
    - frontend/src/routes/alerts/index.tsx contains `createFileRoute('/alerts/')`
    - `npx vite build` succeeds (routeTree.gen.ts regenerated, TypeScript compiles)
  </acceptance_criteria>
  <done>All TanStack Query hooks created for every Phase 3 API endpoint. Alert hooks use 30-second polling per D-12. Route scaffolds created and routeTree.gen.ts regenerated. Frontend builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | Frontend sends JWT Bearer token; apiClient handles injection |
| API response -> Browser state | Decrypted PHI (patient name, MRN) lives in browser memory via TanStack Query cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | TanStack Query cache | accept | PHI in browser memory is acceptable — user is already authenticated and authorized; cache clears on logout/tab close. No localStorage persistence of PHI. |
| T-03-06 | Spoofing | apiClient JWT injection | mitigate | `getAccessToken()` from auth.ts provides Keycloak JWT; 401 responses handled by apiClient throwing ApiError; Keycloak session expiry forces re-auth |
</threat_model>

<verification>
1. `npx vite build` succeeds from frontend directory
2. All 3 type files contain interfaces matching backend DTOs
3. All 3 api.ts files import from `@/lib/api-client`
4. Alert hooks use `refetchInterval: 30_000`
5. 4 route scaffold files exist and follow `createFileRoute` pattern
6. New shadcn components (select, textarea, table) exist in components/ui/
</verification>

<success_criteria>
- Frontend builds successfully with all new types, hooks, and routes
- Every backend endpoint has a corresponding TanStack Query hook
- Polling configured at 30-second intervals for alerts and dashboard per D-12
- Route files establish the URL structure: /patients, /patients/new, /patients/$patientId, /alerts
- shadcn components available for Plans 04 and 05
</success_criteria>

<output>
After completion, create `.planning/phases/03-working-application/03-02-SUMMARY.md`
</output>
