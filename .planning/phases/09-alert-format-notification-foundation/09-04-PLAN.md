---
phase: 09-alert-format-notification-foundation
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - src/test/java/com/onconavigator/notification/LoggingNotificationServiceTest.java
  - src/test/java/com/onconavigator/notification/NotificationPreferenceServiceTest.java
  - src/test/java/com/onconavigator/activity/DigestDispatchActivityImplTest.java
  - src/test/java/com/onconavigator/ai/service/AlertGenerationAiServiceTest.java
autonomous: true
requirements:
  - PW-ALL-007
  - PW-ALL-004

must_haves:
  truths:
    - "150-char enforcement is tested: values exceeding 150 are truncated"
    - "Severity filtering logic is tested: empty filter passes all, non-empty filters correctly"
    - "Quiet hours detection is tested: before/after/wrapping-midnight cases"
    - "Admin-default to user-override merge is tested"
    - "Digest queue drain is tested: items dispatched via NotificationService.dispatchFromQueue, marked DISPATCHED"
    - "MISSING_SUMMARY parsing from Claude response is tested"
    - "Fallback derivation of missingSummary from deviationDescription is tested"
  artifacts:
    - path: "src/test/java/com/onconavigator/notification/LoggingNotificationServiceTest.java"
      provides: "Unit tests for dispatch routing, severity filter, quiet hours, digest queueing"
      contains: "class LoggingNotificationServiceTest"
    - path: "src/test/java/com/onconavigator/notification/NotificationPreferenceServiceTest.java"
      provides: "Unit tests for preference merge, severity filter, quiet hours logic"
      contains: "class NotificationPreferenceServiceTest"
    - path: "src/test/java/com/onconavigator/activity/DigestDispatchActivityImplTest.java"
      provides: "Unit tests for pending queue drain and dispatch via NotificationService"
      contains: "class DigestDispatchActivityImplTest"
    - path: "src/test/java/com/onconavigator/ai/service/AlertGenerationAiServiceTest.java"
      provides: "Unit tests for MISSING_SUMMARY parsing and 150-char cap"
      contains: "missingSummary"
  key_links:
    - from: "src/test/java/com/onconavigator/notification/LoggingNotificationServiceTest.java"
      to: "src/main/java/com/onconavigator/notification/LoggingNotificationService.java"
      via: "Tests dispatch routing logic"
      pattern: "dispatchForAlert"
    - from: "src/test/java/com/onconavigator/activity/DigestDispatchActivityImplTest.java"
      to: "src/main/java/com/onconavigator/notification/NotificationService.java"
      via: "Verifies delegation to NotificationService.dispatchFromQueue"
      pattern: "dispatchFromQueue"
    - from: "src/test/java/com/onconavigator/ai/service/AlertGenerationAiServiceTest.java"
      to: "src/main/java/com/onconavigator/ai/service/AlertGenerationAiService.java"
      via: "Tests MISSING_SUMMARY parsing"
      pattern: "MISSING_SUMMARY"
---

<objective>
Create unit tests covering the critical Phase 9 logic: 150-char enforcement, notification preference merge, severity filtering, quiet hours detection, digest queue drain (via NotificationService delegation), and Claude MISSING_SUMMARY parsing.

Purpose: Verify the correctness of the notification routing logic and the two-part alert format enforcement. These tests catch regressions in preference merge priority (user overrides admin), quiet-hours boundary conditions (midnight wrapping), and Claude response parsing edge cases.

Output: 4 test files with comprehensive coverage of notification infrastructure logic.
</objective>

<execution_context>
@.planning/phases/09-alert-format-notification-foundation/09-PATTERNS.md
@.planning/phases/09-alert-format-notification-foundation/09-02-SUMMARY.md
@.planning/phases/09-alert-format-notification-foundation/09-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces being tested. -->

From src/main/java/com/onconavigator/notification/NotificationPreferenceService.java:
```java
public Optional<NotificationPreference> getEffectivePreference(UUID userId, NotificationChannel channel);
public boolean passesSeverityFilter(NotificationPreference pref, String alertTypeName);
public boolean isInQuietHours(NotificationPreference pref);
public OffsetDateTime computeQuietHoursEnd(NotificationPreference pref);
```

From src/main/java/com/onconavigator/notification/LoggingNotificationService.java:
```java
@Override
public void dispatchForAlert(Alert alert, String patientName, String patientMrn);
@Override
public void dispatchFromQueue(UUID alertId, UUID userId, NotificationChannel channel,
                              String renderedContent, boolean isDigest);
```

From src/main/java/com/onconavigator/activity/DigestDispatchActivityImpl.java:
```java
@Override
public void drainPendingQueue();
// Delegates to: notificationService.dispatchFromQueue(alertId, userId, channel, renderedContent, isDigest)
```

From src/main/java/com/onconavigator/ai/service/AlertGenerationAiService.java:
```java
// parseAlertResponse is private -- test via public generateAlertDescription with mocked ChatClient
// OR use reflection-based testing if preferred
// Better approach: test the public API and verify the AlertText output has missingSummary
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationPreferenceServiceTest and LoggingNotificationServiceTest</name>
  <read_first>
    - src/main/java/com/onconavigator/notification/NotificationPreferenceService.java
    - src/main/java/com/onconavigator/notification/LoggingNotificationService.java
    - src/main/java/com/onconavigator/domain/NotificationPreference.java
    - src/main/java/com/onconavigator/domain/Alert.java
    - src/main/java/com/onconavigator/domain/enums/NotificationChannel.java
    - src/main/java/com/onconavigator/domain/enums/AlertType.java
    - src/test/java/com/onconavigator/activity/PathwayEvaluationActivityImplTest.java
  </read_first>
  <files>
    src/test/java/com/onconavigator/notification/NotificationPreferenceServiceTest.java,
    src/test/java/com/onconavigator/notification/LoggingNotificationServiceTest.java
  </files>
  <action>
**NotificationPreferenceServiceTest.java** -- Create in `src/test/java/com/onconavigator/notification/`. Use `@ExtendWith(MockitoExtension.class)` with `@Mock NotificationPreferenceRepository`. Follow the pattern from existing tests in the project. Test methods:

1. `getEffectivePreference_userOverrideExists_returnsUserPreference` -- Set up repo to return a user-specific preference; verify it is returned (not the admin default).

2. `getEffectivePreference_noUserOverride_returnsAdminDefault` -- Set up repo with no user preference but with an admin default for the channel; verify admin default is returned.

3. `getEffectivePreference_neitherExists_returnsEmpty` -- Neither user nor admin default exists; verify Optional.empty().

4. `passesSeverityFilter_emptyFilter_passesAll` -- Create a preference with empty `alertTypeFilter`; verify `passesSeverityFilter` returns true for any alert type.

5. `passesSeverityFilter_filterContainsType_passes` -- Create a preference with `alertTypeFilter = ["MISSING_EVENT", "DELAYED_EVENT"]`; verify MISSING_EVENT passes.

6. `passesSeverityFilter_filterDoesNotContainType_fails` -- Same filter; verify CANCELLED_EVENT fails.

7. `isInQuietHours_normalRange_currentInside_returnsTrue` -- quietHoursStart=9, quietHoursEnd=17, current hour simulated as 12; returns true. NOTE: Since `isInQuietHours` uses `LocalTime.now(zone)`, this test needs to either use a timezone where the current time is known OR the test should be structured to pick a timezone where the local hour is within range. Alternative: extract time-source dependency. For simplicity, test by setting quiet hours to span the current hour (e.g., 0-24 always matches).

8. `isInQuietHours_wrappingMidnight_currentInside_returnsTrue` -- quietHoursStart=22, quietHoursEnd=7. Test with appropriate assertions.

9. `isInQuietHours_noQuietHoursSet_returnsFalse` -- quietHoursStart=null; returns false.

**LoggingNotificationServiceTest.java** -- Create in `src/test/java/com/onconavigator/notification/`. Use `@ExtendWith(MockitoExtension.class)` with mocks for `NotificationPreferenceService`, `NotificationLogRepository`, `NotificationPendingQueueRepository`. Use `@InjectMocks LoggingNotificationService`. Set the `baseUrl` field via reflection or `ReflectionTestUtils.setField`. Test methods:

1. `dispatchForAlert_noEnabledPreferences_noDispatch` -- preferenceService.getAllEnabledPreferences returns empty list; verify no save calls.

2. `dispatchForAlert_enabledPreference_severityFiltered_noDispatch` -- preference exists but severity filter rejects the alert type; verify no log entry saved.

3. `dispatchForAlert_enabledPreference_immediateDispatch_logsAndSaves` -- preference exists, severity passes, not in quiet hours, not digest mode; verify `notificationLogRepository.save()` is called with correct alert ID, user ID, channel.

4. `dispatchForAlert_enabledPreference_quietHoursActive_queuesNotification` -- preference exists, severity passes, in quiet hours; verify `pendingQueueRepository.save()` called with holdType="QUIET_HOURS".

5. `dispatchForAlert_enabledPreference_digestMode_queuesNotification` -- preference has digestEnabled=true; verify `pendingQueueRepository.save()` called with holdType="DIGEST".

6. `dispatchForAlert_adminDefaultWithNullUserId_skipped` -- preference has userId=null (admin default row); verify no dispatch attempted for it.

7. `dispatchFromQueue_savesNotificationLog` -- Call `dispatchFromQueue()` with test params; verify `notificationLogRepository.save()` is called with correct alertId, userId, channel, renderedContent, and isDigest flag.

Use `@MockitoBean` (Spring Boot 3.4+ pattern) if running in a Spring test context, or pure Mockito with `@Mock`/`@InjectMocks` for unit tests. Prefer pure Mockito for speed.
  </action>
  <verify>
    <automated>cd /Users/csharpl/Desktop/Source_Code/Java/Onco-Navigator && ./mvnw test -pl . -Dtest="NotificationPreferenceServiceTest,LoggingNotificationServiceTest" -DfailIfNoTests=false 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - NotificationPreferenceServiceTest.java contains "class NotificationPreferenceServiceTest"
    - NotificationPreferenceServiceTest.java contains "getEffectivePreference_userOverrideExists"
    - NotificationPreferenceServiceTest.java contains "passesSeverityFilter_emptyFilter"
    - NotificationPreferenceServiceTest.java contains "isInQuietHours"
    - LoggingNotificationServiceTest.java contains "class LoggingNotificationServiceTest"
    - LoggingNotificationServiceTest.java contains "dispatchForAlert_enabledPreference_immediateDispatch"
    - LoggingNotificationServiceTest.java contains "dispatchForAlert_enabledPreference_quietHoursActive"
    - LoggingNotificationServiceTest.java contains "dispatchForAlert_enabledPreference_digestMode"
    - LoggingNotificationServiceTest.java contains "dispatchFromQueue_savesNotificationLog"
    - Both test classes pass when run with `./mvnw test -Dtest="NotificationPreferenceServiceTest,LoggingNotificationServiceTest"`
  </acceptance_criteria>
  <done>NotificationPreferenceServiceTest has 9 test methods covering preference merge, severity filter, and quiet hours. LoggingNotificationServiceTest has 7 test methods covering dispatch routing and dispatchFromQueue. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: DigestDispatchActivityImplTest and AlertGenerationAiServiceTest MISSING_SUMMARY tests</name>
  <read_first>
    - src/main/java/com/onconavigator/activity/DigestDispatchActivityImpl.java
    - src/main/java/com/onconavigator/ai/service/AlertGenerationAiService.java
    - src/main/java/com/onconavigator/ai/model/AlertText.java
    - src/main/java/com/onconavigator/domain/NotificationPendingQueue.java
    - src/test/java/com/onconavigator/activity/PathwayEvaluationActivityImplTest.java
    - src/test/java/com/onconavigator/ai/service/AlertGenerationAiServiceTest.java
  </read_first>
  <files>
    src/test/java/com/onconavigator/activity/DigestDispatchActivityImplTest.java,
    src/test/java/com/onconavigator/ai/service/AlertGenerationAiServiceTest.java
  </files>
  <action>
**DigestDispatchActivityImplTest.java** -- Create in `src/test/java/com/onconavigator/activity/`. Use `@ExtendWith(MockitoExtension.class)` with mocks for `NotificationPendingQueueRepository` and `NotificationService`. Note: this test mocks `NotificationService` (not NotificationLogRepository) since DigestDispatchActivityImpl now delegates dispatch to the service. Test methods:

1. `drainPendingQueue_noReadyItems_doesNothing` -- Repository returns empty list; verify no calls to `notificationService.dispatchFromQueue()`.

2. `drainPendingQueue_quietHoursItem_dispatchesViaService` -- Create a NotificationPendingQueue with holdType="QUIET_HOURS" and holdUntil in the past; verify `notificationService.dispatchFromQueue()` is called with correct params (alertId, userId, channel, renderedContent, isDigest=false) and the item is marked "DISPATCHED" via `pendingQueueRepository.save()`.

3. `drainPendingQueue_digestItems_groupedByUser_dispatchesViaService` -- Create 3 pending items (2 for user A, 1 for user B) all with holdType="DIGEST"; verify `notificationService.dispatchFromQueue()` is called 3 times with isDigest=true, and all 3 items are marked DISPATCHED.

4. `drainPendingQueue_mixedTypes_allProcessed` -- Mix of QUIET_HOURS and DIGEST items; verify total dispatchFromQueue call count matches item count.

For test setup: Create `NotificationPendingQueue` instances manually (constructor + setters). Set `holdUntil` to `OffsetDateTime.now().minusMinutes(5)` to ensure they qualify. Mock `findByStatusAndHoldUntilBefore` to return the list.

**AlertGenerationAiServiceTest.java** -- If this file already exists, ADD new test methods. If not, create it. Use `@ExtendWith(MockitoExtension.class)` with `@Mock ChatClient` (or the appropriate mock structure for Spring AI ChatClient). The key is testing the `parseAlertResponse` method. Since it's private, test it through the public `generateAlertDescription` method by mocking the ChatClient to return specific response strings. Test methods:

1. `generateAlertDescription_fullResponse_parsesMissingSummary` -- Mock ChatClient to return:
```
DESCRIPTION: Step X has not been completed within the expected timeframe.
SUGGESTED_ACTION: Contact facility to confirm scheduling.
MISSING_SUMMARY: CT scan not completed within 14 days of referral.
```
Verify the returned AlertText has all three fields populated.

2. `generateAlertDescription_noMissingSummarySection_derivesFromDescription` -- Mock ChatClient to return only DESCRIPTION and SUGGESTED_ACTION (no MISSING_SUMMARY line). Verify missingSummary is derived from the first 150 chars of description.

3. `generateAlertDescription_missingSummaryExceeds150_truncated` -- Mock ChatClient to return a MISSING_SUMMARY longer than 150 characters. Verify the returned missingSummary is exactly 150 chars.

4. `generateAlertDescription_suggestedActionExceeds150_truncated` -- Mock ChatClient to return a SUGGESTED_ACTION longer than 150 characters. Verify it is truncated to 150 chars.

5. `generateAlertDescription_fallback_returnsNull` -- When circuit breaker is triggered, verify null is returned (existing test may cover this; if so, just verify it still passes).

NOTE: The ChatClient mock structure depends on Spring AI's API. The pattern is:
```java
// Mock chain: alertClient.prompt().user(any()).call().content() returns responseString
when(alertClient.prompt()).thenReturn(promptSpec);
when(promptSpec.user(any(Consumer.class))).thenReturn(promptSpec);
when(promptSpec.call()).thenReturn(callResponse);
when(callResponse.content()).thenReturn(responseString);
```
Look at the existing `AlertGenerationAiServiceTest.java` for the exact mock pattern already in use.
  </action>
  <verify>
    <automated>cd /Users/csharpl/Desktop/Source_Code/Java/Onco-Navigator && ./mvnw test -pl . -Dtest="DigestDispatchActivityImplTest,AlertGenerationAiServiceTest" -DfailIfNoTests=false 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DigestDispatchActivityImplTest.java contains "class DigestDispatchActivityImplTest"
    - DigestDispatchActivityImplTest.java contains "drainPendingQueue_noReadyItems"
    - DigestDispatchActivityImplTest.java contains "drainPendingQueue_quietHoursItem"
    - DigestDispatchActivityImplTest.java contains "drainPendingQueue_digestItems_groupedByUser"
    - DigestDispatchActivityImplTest.java contains "notificationService.dispatchFromQueue" or "verify(notificationService)"
    - AlertGenerationAiServiceTest.java contains "parsesMissingSummary" or "missingSummary"
    - AlertGenerationAiServiceTest.java contains "derivesFromDescription" or "noMissingSummarySection"
    - AlertGenerationAiServiceTest.java contains "truncated" or "Exceeds150"
    - Both test classes pass when run with `./mvnw test -Dtest="DigestDispatchActivityImplTest,AlertGenerationAiServiceTest"`
  </acceptance_criteria>
  <done>DigestDispatchActivityImplTest has 4 test methods covering empty queue, quiet-hours dispatch via service, digest grouping via service, and mixed types. AlertGenerationAiServiceTest has tests for MISSING_SUMMARY parsing, fallback derivation, and 150-char truncation. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> production code | Tests verify security properties (150-char cap, PHI handling) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | Missing test coverage for 150-char bypass | mitigate | Explicit tests verify truncation at 150 chars for both missingSummary and suggestedAction |
| T-09-14 | Information Disclosure | Test fixtures containing PHI | accept | Tests use synthetic "Test Patient" / "MRN001" values; no real PHI in test code |
</threat_model>

<verification>
- `./mvnw test -pl . -Dtest="NotificationPreferenceServiceTest,LoggingNotificationServiceTest,DigestDispatchActivityImplTest,AlertGenerationAiServiceTest"` passes
- All 4 test files exist with the specified test methods
- Tests cover: preference merge, severity filter, quiet hours, dispatch routing, queue drain via NotificationService, MISSING_SUMMARY parsing, 150-char cap
</verification>

<success_criteria>
All unit tests pass. Coverage includes: notification preference admin-to-user merge logic, severity filter with empty/populated arrays, quiet hours boundary detection (normal/wrapping/disabled), immediate vs queued dispatch routing, dispatchFromQueue delegation, digest queue grouping and drain, Claude MISSING_SUMMARY parsing with fallback, and 150-character truncation enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/09-alert-format-notification-foundation/09-04-SUMMARY.md`
</output>
